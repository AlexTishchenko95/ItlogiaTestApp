import { ItlInputDirective } from './itl-input.directive';

describe('ItlInputDirective', () => {
  it('should create an instance', () => {
    const directive = new ItlInputDirective();
    expect(directive).toBeTruthy();
  });
});
